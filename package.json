{
  "name": "portfolio",
  "version": "2.0.0",
  "description": "",
  "repository": {},
  "main": "index.js",
  "dependencies": {
    "autoprefixer": "^10.1.0",
    "gray-matter": "^4.0.2",
    "next": "^12.1.0",
    "postcss": "^8.2.1",
    "raw-loader": "^4.0.2",
    "react": "^17.0.1",
    "react-dom": "^17.0.1",
    "react-markdown": "^5.0.3",
    "tailwindcss": "^2.0.2"
  },
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "export": "next export",
    "export-dist": "next export -o dist/",
    "local_deploy": "cross-env NODE_ENV=development next build && next export -o dist/",
    "build_deploy": "NODE_ENV=production next build && next export -o dist/"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "cross-env": "^7.0.3"
  }
}
