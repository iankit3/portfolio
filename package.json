{
  "name": "portfolio",
  "version": "2.0.0",
  "description": "",
  "repository": {},
  "main": "index.js",
  "dependencies": {
    "autoprefixer": "^10.1.0",
    "next": "^10.0.4",
    "postcss": "^8.2.1",
    "react": "^17.0.1",
    "react-dom": "^17.0.1",
    "tailwindcss": "^2.0.2"
  },
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "local_deploy": "cross-env NODE_ENV=dev next build && next export -o dist/",
    "build_deploy": "NODE_ENV=production next build && next export -o dist/"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "cross-env": "^7.0.3"
  }
}
