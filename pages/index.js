import Page from "../Components/Page";

function HomePage({ selectedRouteIndex }) {
  return (
    <div>
      <b>UNDER CONSTRUCSION !!!</b>
      <div>Welcome to my portfolio website {selectedRouteIndex}</div>
    </div>
  );
}

export default Page(HomePage);
